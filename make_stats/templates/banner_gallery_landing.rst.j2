Banner Gallery
==============

A gallery of ANSI connection banners collected from responding
{{ entity_name }}. Servers that display identical visible banner text are
grouped together, and servers with near-identical banners (95%+
similar) are listed alongside. Each group shows the shared banner
image and a list of all servers in that group.

{{ total_groups }} unique banners across {{ total_servers }} servers.

.. toctree::
   :maxdepth: 1

{% for p_num, label in page_labels %}
   Page {{ p_num }} {{ label }} <banner_gallery_{{ p_num }}>
{% endfor %}

